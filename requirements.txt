# BatchPrint Pro - 批量打印软件依赖清单
# 使用 pip install -r requirements.txt 安装所有依赖

# GUI 框架
PySide6==6.11.1

# 图片处理
Pillow>=10.0.0

# PDF 处理
PyMuPDF>=1.23.0

# Windows API 和 COM 接口
pywin32>=306

# Word 文档处理（备用）
python-docx>=0.8.11

# 打包工具（可选，用于打包为 exe）
# PyInstaller>=6.0.0
# Nuitka>=1.9.0
